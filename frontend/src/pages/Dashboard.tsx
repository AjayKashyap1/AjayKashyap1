import { useQuery } from "@tanstack/react-query";import { Activity, AlertTriangle, Boxes, MapPin } from "lucide-react";import { api } from "../api/client";import { StatCard } from "../components/StatCard";import type { Dashboard as DashboardType } from "../types";
export function Dashboard(){const {data,isLoading}=useQuery({queryKey:["dashboard"],queryFn:async()=> (await api.get<DashboardType>("/dashboard")).data});if(isLoading)return <p>Loading intelligence...</p>;const d=data!;return <div><header><p className="text-brand">Blinkit seller intelligence</p><h2 className="text-4xl font-black text-white">Executive Dashboard</h2></header><section className="mt-8 grid gap-4 md:grid-cols-2 xl:grid-cols-4"><StatCard label="Total Products" value={d.totalProducts} icon={<Boxes/>}/><StatCard label="Coverage" value={`${d.coveragePercent}%`} icon={<Activity/>}/><StatCard label="Cities Live" value={d.citiesLive} icon={<MapPin/>}/><StatCard label="Out of Stock" value={d.outOfStock} icon={<AlertTriangle/>}/></section><section className="mt-8 grid gap-6 xl:grid-cols-3"><div className="glass rounded-3xl p-6 xl:col-span-2"><h3 className="text-xl font-bold">Top Cities</h3><div className="mt-5 grid gap-3">{d.topCities.map(c=><div className="flex justify-between rounded-2xl bg-white/5 p-3" key={c.name}><span>{c.name}</span><strong>{c.observations}</strong></div>)}</div></div><div className="glass rounded-3xl p-6"><h3 className="text-xl font-bold">Latest Alerts</h3><div className="mt-5 grid gap-3">{d.latestAlerts.map(a=><article className="rounded-2xl bg-white/5 p-3" key={a.id}><b className="capitalize text-brand">{a.severity}</b><p className="text-sm text-slate-300">{a.message}</p></article>)}</div></div></section></div>}
