export function ModulePage({title}:{title:string}){return <section className="glass rounded-[2rem] p-8"><p className="text-brand">QC Intel module</p><h2 className="mt-2 text-4xl font-black text-white">{title}</h2><p className="mt-4 max-w-3xl text-slate-300">Authenticated workspace for {title.toLowerCase()} operations, governed by the same tenant-aware API, role model, background queue, audit logging and reporting foundation used across QC Intel.</p></section>}
