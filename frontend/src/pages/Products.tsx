import { useQuery } from "@tanstack/react-query";import { api } from "../api/client";import type { Product } from "../types";
export function Products(){const {data=[]}=useQuery({queryKey:["products"],queryFn:async()=> (await api.get<Product[]>("/products")).data});return <div><h2 className="text-4xl font-black text-white">Products</h2><div className="mt-8 overflow-hidden rounded-3xl glass"><table className="w-full text-left"><thead className="bg-white/10"><tr><th className="p-4">SKU</th><th>Brand</th><th>Category</th><th>Status</th><th>Blinkit ID</th></tr></thead><tbody>{data.map(p=><tr className="border-t border-white/10" key={p.id}><td className="p-4 font-semibold">{p.sku}</td><td>{p.brand}</td><td>{p.category}</td><td>{p.status}</td><td>{p.blinkit_product_id}</td></tr>)}</tbody></table></div></div>}
