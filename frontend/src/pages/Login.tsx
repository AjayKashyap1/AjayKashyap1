import { FormEvent, useState } from "react";import { api } from "../api/client";
export function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [remember,setRemember]=useState(true);const [error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();setError("");try{const {data}=await api.post("/auth/login",{email,password,remember_me:remember});localStorage.setItem("qcintel.token",data.access_token);location.href="/"}catch{setError("Invalid email or password")}}return <main className="grid min-h-screen place-items-center p-6"><form onSubmit={submit} className="glass w-full max-w-md rounded-[2rem] p-8"><h1 className="text-3xl font-bold text-white">QC Intel</h1><p className="mt-2 text-slate-300">Sign in to quick commerce intelligence.</p><label className="mt-8 block text-sm">Email<input className="mt-2 w-full rounded-2xl border border-slate-700 bg-slate-950/70 p-3" value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><label className="mt-4 block text-sm">Password<input className="mt-2 w-full rounded-2xl border border-slate-700 bg-slate-950/70 p-3" value={password} onChange={e=>setPassword(e.target.value)} type="password" minLength={8} required/></label><label className="mt-4 flex gap-2 text-sm"><input type="checkbox" checked={remember} onChange={e=>setRemember(e.target.checked)}/>Remember me</label>{error&&<p className="mt-4 text-red-300">{error}</p>}<button className="mt-6 w-full rounded-2xl bg-brand p-3 font-semibold text-ink">Login</button></form></main>}
